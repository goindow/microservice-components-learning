server:
  port: 8081
spring:
  application:
    name: microservice-gateway-user-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848    # standalon
#        server-addr: localhost:8848,localhost:8849,localhost:8850    # cluster
#      username: nacos
#      password: nacos