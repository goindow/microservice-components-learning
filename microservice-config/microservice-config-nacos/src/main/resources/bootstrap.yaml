spring:
  cloud:
    nacos:
      # discovery 和 config 可以配合使用，也可以分开使用
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml    # 配置中心存储 yaml 格式的配置文件
#      username: nacos
#      password: nacos